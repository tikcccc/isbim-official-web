apiVersion: v1
kind: Service
metadata:
  name: isbim-web
  namespace: isbim-web
  labels:
    app.kubernetes.io/name: isbim-web
    app.kubernetes.io/part-of: isbim
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: isbim-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
